/**
 * MSW handlers for API mocking in tests
 * Add your API endpoint handlers here
 */
export const handlers = [
  // Example: Mock auth endpoints
  // http.post('/api/auth/login', () => {
  //   return HttpResponse.json({ user: { id: '1', email: 'test@example.com' } });
  // }),
  // Example: Mock recipes endpoints
  // http.get('/api/recipes', () => {
  //   return HttpResponse.json({ recipes: [] });
  // }),
];
